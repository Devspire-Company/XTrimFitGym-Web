# Copy to ".env" in this folder (Expo loads EXPO_PUBLIC_* automatically).
# The publishable key MUST pair with CLERK_SECRET_KEY on your API (same Clerk app in Dashboard → API keys).
# If they are from different instances, sign-in works in the app but GraphQL `me` returns null.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
